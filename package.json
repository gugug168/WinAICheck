{
  "name": "aicoevo",
  "version": "0.1.0",
  "description": "AI environment diagnostic CLI tool for Windows",
  "type": "module",
  "scripts": {
    "dev": "bun run src/main.ts -- --cli",
    "build": "bun run scripts/prebuild.ts && bun build --compile --minify src/main.ts --outfile dist/aicoevo.exe",
    "test": "bun test"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "typescript": "^5.5.0"
  }
}
