{
  "name": "aicoevo",
  "version": "0.1.0",
  "description": "AI environment diagnostic CLI tool for Windows",
  "type": "module",
  "scripts": {
    "dev": "bun run src/main.tsx -- --no-tui",
    "build": "bun run scripts/prebuild.ts && bun build --compile --minify --external react-devtools-core src/main.tsx --outfile dist/aicoevo.exe",
    "test": "bun test"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "cli-table3": "^0.6.5",
    "ink": "^5.1.0",
    "react": "^18.3.1"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "@types/react": "^18.3.0",
    "typescript": "^5.5.0"
  }
}
